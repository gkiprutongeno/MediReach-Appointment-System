{
  "name": "medireach-root",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "postinstall": "npm --prefix client install && npm --prefix server install || true",
    "build": "npm --prefix client run build",
    "start": "npm --prefix server run start"
  }
}
